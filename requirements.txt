# Kalkulator Dachów v5.0 - Dependencies
# Install with: pip install -r requirements.txt

# PDF generation (patched for CVE)
reportlab>=3.6.13

# Image handling (logos) - patched for CVEs
Pillow>=10.2.0

# Excel export
openpyxl>=3.0.0
